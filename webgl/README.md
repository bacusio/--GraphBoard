# 关系图谱白板 MVP

这是一个本地优先的通用关系图谱白板原型。

已实现：

- 无限 SVG 画布、平移与缩放；
- 批量导入头像；
- 通用对象卡片和阵营区域；
- 拖动、网格吸附和基础排版；
- 从头像边缘建立关系；
- 基于距离与拖动方向的目标预判；
- 有向、无向、双向关系；
- 关系标签、颜色和线型；
- 端点重新连接；
- 撤销和重做；
- 本地自动恢复；
- JSON 项目保存与打开；
- PNG 导出。

运行方式：使用本地静态服务器打开 `index.html`。Windows 交付目录将包含启动器，无需用户手动启动服务器。

